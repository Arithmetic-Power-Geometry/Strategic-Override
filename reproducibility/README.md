# Reproducibility

Run `python reproduce.py` from the repository root, then run `python reproducibility/verify_reproduction.py`. The verifier compares regenerated outputs with the declared primary results and checks the five-state partition. GitHub Actions performs both steps automatically.
